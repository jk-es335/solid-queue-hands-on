<%= form_with(model: job_completion) do |form| %>
  <% if job_completion.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(job_completion.errors.count, "error") %> prohibited this job_completion from being saved:</h2>

      <ul>
        <% job_completion.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :adapter, style: "display: block" %>
    <%= form.text_field :adapter %>
  </div>

  <div>
    <%= form.label :completed_at, style: "display: block" %>
    <%= form.datetime_field :completed_at %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
